#Benefits.gov origin-staging to origin-www

# run with "wraith capture configs/BG/BG_origin-staging_to_origin-www.yaml"

imports: "settings_BG.yaml"

# (required) The domains to take screenshots of.
domains:
  origin-staging: "https://origin-staging.benefits.gov"
  origin-www: "https://origin-www.benefits.gov"
